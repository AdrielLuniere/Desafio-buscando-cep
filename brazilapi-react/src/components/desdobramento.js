export const Desdobramento = ({corretora}) => {
    return (
        <div>
        <span>{corretora.nome_social}</span>
        <span>{corretora.cnpj}</span>
        <span>{corretora.municipio}</span>
        </div>
    )
}

